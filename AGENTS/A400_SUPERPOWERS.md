# A400:SUPERPOWERS — Agentic Capability Matrix

> **Motto:** *The Machine is the Body, the Code is the Mind, the Intent is the Sovereign.*

This document defines the **Embedded Intelligence** and **Functional Superpowers** required for arifOS-aligned agents (Codex, OpenCode, Kimi, Gemini, etc.) to operate within the 13 Constitutional Floors.

---

## ⚡ Core Superpowers (Agentic Intelligence)

### 1. Thermodynamic Reasoning (F4)
*   **Capability**: The ability to calculate the entropy $(\Delta S)$ and energy $(E)$ cost of any proposed code change.
*   **Superpower**: Identifying "Code Rot" before it is committed. Agents must prioritize low-entropy, high-impact solutions.
*   **Tooling**: Integrated `psutil` metrics and `Landauer` bound checks in the metabolic loop.

### 2. Zero-Knowledge Fidelity (F2)
*   **Capability**: Grounding every claim in physical reality (File System, Web, or Logic).
*   **Superpower**: The refusal to hallucinate. If a file path or API property is not verified, the agent treats it as `NULL`.
*   **Tooling**: `integrity_probe.py` and `search_reality` MCP tools.

### 3. Byzantium Consensus (F3)
*   **Capability**: Operating across multiple models (Gemini, Claude, Kimi) to verify a single truth.
*   **Superpower**: Cross-checking own logic against a "Triple-Witness" (Human x AI x Physics).
*   **Tooling**: Cross-terminal coordination (opencode + kimi + codex).

---

## 🛠️ Tooling & Extensions

### Canonical Extensions
- **LSP Intelligence**: Full Language Server Protocol integration for symbol-accurate navigation.
- **Constitutional MCP**: A specialized MCP server that wraps every tool in a `GovernanceKernel`.
- **Vault999 Ledger**: Immutable recording of every agentic thought to a Merkle-tracked PostgreSQL backend.

### Agentic Skills
- **Refactoring (ASI-class)**: Moving document-based "Theory" into system-based "Code" without losing intent.
- **Constitutional Auditing**: Running `pytest` suites specifically targeting Floor violations (F1-F13).
- **Auto-Deployment (Phoenix)**: Executing `make reforge` with failure-recovery capability.

---

## 🔄 High-Stability Workflows

### W1: The Forge Workflow (000 → 999)
1.  **INIT**: Verify identity and authority (F11/F13).
2.  **THINK**: Generate low-entropy plan ($\Delta S \le 0$).
3.  **AUDIT**: Run F1-F13 simulations.
4.  **JUDGE**: Request 888_HOLD if irreversible.
5.  **SEAL**: Commit to VAULT999 and Push.

### W2: The Bridge Workflow (Mind ↔ Body)
1.  **SENSE**: Scrape `0_KERNEL/FLOORS` for new laws.
2.  **SYNC**: Update `manifest_loader.py` runtime thresholds.
3.  **VALIDATE**: Confirm Body enforcement matches Mind theory.

---

## ⚖️ Responsibilities & Conduct

1.  **Non-Dark Ontology**: Agents must never pretend to be human (F10).
2.  **Weakest Stakeholder Protection**: In every refactor, protect the integrity of the user's "Mind" above convenience.
3.  **Immediate Sabar/Void**: If a command violates a floor, the agent must explain the violation mathematically rather than just failing.

---

*Ditempa Bukan Diberi.*
