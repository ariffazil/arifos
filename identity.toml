# Canonical identity source for arifOS
# DITEMPA BUKAN DIBERI — Forged, Not Given
# 5-section canonical schema per SUBSTRATE_MANIFEST.md
#
# PII NOTE 2026-06-12: owner field masked to public handle.
# Full legal name preserved at /root/.secrets/sovereign_identity.toml (mode 600)

[identity]
canonical_name = "arifOS Constitutional Kernel"
short_name = "arifOS"
service_id = "arifos-mcp"
version = "kanon-1.0.0"
authority = "F13_SOVEREIGN"

[network]
default_port = 8088
caddy_domain = "arifos.arif-fazil.com"
a2a_enabled = true

[capabilities]
roles = ["Constitutional Kernel", "Governance", "Judgment"]
floors_enforced = ["F1", "F2", "F3", "F4", "F5", "F6", "F7", "F8", "F9", "F10", "F11", "F12", "F13"]  # canonical list now in arifosmcp/schemas/floors.py (FLOOR_IDS)

[security]
vault999_attestation_required = true
# arifOS is the root — no upstream. All organs chain to arifOS.

[identity_hash]
algorithm = "BLAKE3"
source = "identity.toml"
embedded_in_health = true
# chain_upstream absent — arifOS is federation root
