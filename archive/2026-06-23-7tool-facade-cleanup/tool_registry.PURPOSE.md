# APEX/ASF1/tool_registry.json — Purpose Label (Ω — 2026-06-07)

> **Role:** MIRROR — APEX-side copy of the canonical arifOS registry
>
> **Tool count:** 13 (should match `/root/arifOS/arifosmcp/tool_registry.json`)
>
> **Used by:** A-FORGE `/api/repo-steward/registry-trinity` (mirror role)

## Drift Signal (2026-06-07 audit)

This file's SHA-256 prefix is `608b8b79d94eccb1`, while the canonical SHA-256 prefix is `81883ff74fd06b87`. Both declare 13 tools but the contents differ — most likely a formatting/whitespace diff, but the file should be re-synced to eliminate the discrepancy.

**Recommended action:** Re-export `tool_registry.json` and copy to `APEX/ASF1/tool_registry.json`, or accept the drift and document it as intentional (e.g. APEX adds its own metadata fields).

DITEMPA BUKAN DIBERI.
