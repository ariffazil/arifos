# 2026-04-06

## Vault999 BLS Architecture Blueprint
- Created `000/THEORY/K999_VAULT.md` defining the cryptographically secure verdict models, key hierarchy (Offline Root Key + VPS Online Signer), and legacy log migrations.
- Objective is moving from unsigned logs to tamper-proof and cryptographically sealed logs.

## External Progress Updates
- Cloudflare SSE timeout issues have been resolved via commit `e91706b` / `e9176ee`. Traefik is now configured with the `mcp-sse-headers` middleware (`FlushInterval=100ms`) and `X-Accel-Buffering: no`, combined with an aggressive keep-alive ping emitted via `SSEKeepAliveMiddleware` (`ping\n\n` every 25s, significantly below CF's 100s idle timeout).
- Ring nav role labels were updated across many front-ends (`arif/index.html`, `aaa/dist/index.html`, `forge/index.html`, `rest_routes.py`, `arifos.arif-fazil.com/index.html`). The `arif-sites` repo auth failed during push and requires attention.

---

## [12:38 +08] DNS Routing Fix — arif-fazil.com ✅ SEALED

**Event:** Targeted Cloudflare DNS fix for `apex` and `waw` subdomains.

**What changed:**
| Record | Before | After |
|--------|--------|-------|
| apex | CNAME → geox.arif-fazil.com (Proxied) | A → 72.62.71.199 (DNS only) |
| waw | CNAME → waw-4b4.pages.dev (Proxied) | A → 72.62.71.199 (DNS only) |

**Page Rules:** Zone had 0/3 rules. No apex redirect rule existed — nothing to delete.

**Untouched (confirmed safe):** arifos CNAME, geox A, aaa, apex2, apex-v2, arifosmcp, forge, all MX/TXT/SPF records.

**Context:** 
- `apex.arif-fazil.com` = THEORY role → now routes directly to VPS 72.62.71.199
- `waw.arif-fazil.com` = STATE role → now routes directly to VPS 72.62.71.199
- Both were previously blocked behind indirect Cloudflare routing (CNAME chain + proxy)

**Blast radius:** Minimal. Only 2 records changed. No email, SSL, worker, cache, or security settings touched.

**Method:** Inline CNAME→A conversion using Cloudflare's DNS editor. User completed manually after subagent hit Turnstile challenge.
