# Cooling Ledger (L1) - Codex Metadata Note

- Ledger path: `cooling_ledger/L1_cooling_ledger.jsonl` (append-only, SHA3-256 hash chain).
- Codex CLI entries add optional metadata: `source` (e.g., `CODEX_CLI`), `task_type`, `task_description`, `scope`, and `codex_audit` (floors, risk_score, entropy_delta, reversible, artifacts).
- Core v35Ω fields remain unchanged; new fields are optional and backward compatible.***
