{
    "description": "arifOS version manifest - canonical record of all sealed releases",
    "format": "JSON map of version to release metadata",
    "schema": {
        "vX.Y.Z": {
            "bundle_hash": "SHA-256 hash of release bundle",
            "commit_hash": "Git commit hash",
            "tag": "Git tag name (e.g. vX.Y.Z)",
            "sealed_at": "ISO-8601 timestamp",
            "human_authority": "Name of approving authority"
        }
    },
    "notes": [
        "This file is updated by /gitseal on APPROVE",
        "Each version entry is immutable once written",
        "bundle_hash provides cryptographic integrity verification",
        "This manifest integrates with vault_999 for version lineage"
    ]
}