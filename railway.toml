[build]
builder = "nixpacks"
buildCommand = "pip install -e ."

[deploy]
startCommand = "sh -c 'export ARIFOS_MCP_MODE=bridge && python -m arifos.mcp trinity-sse'"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
numReplicas = 2
