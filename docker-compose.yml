# arifOS v49.1 - Constitutional Infrastructure Entry Point
# Authority: 888 Judge | Constitutional Canon
# 
# This file serves as a constitutional anchor to the canonical infrastructure.
# All infrastructure configuration is governed by constitutional law in 000_THEORY/.
#
# Constitutional Chain: 888 Judge > 000_THEORY/ > config/infrastructure/
#
# DITEMPA BUKAN DIBERI - Forged through constitutional law, not given through assumption.

version: "3.8"

# Constitutional delegation to canonical infrastructure
# This delegates to the BBB (Machine Memory) layer implementation
services:
  # Infrastructure services are defined in config/infrastructure/docker-compose.yml
  # This entry point maintains constitutional separation of concerns.

# Constitutional Note: 
# - CCC (000_THEORY/): Constitutional canon (immutable law)
# - BBB (config/): Machine-processable specifications 
# - AAA (human agency): Protected human domain
#
# For full infrastructure deployment:
# docker-compose -f config/infrastructure/docker-compose.yml up -d
#
# For VAULT-999 memory infrastructure:
# docker-compose -f config/infrastructure/docker-compose-vault999.yml up -d