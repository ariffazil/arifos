# 📐 arifOS Capabilities & Tool Surface Mapping
**Version:** 2026.03.24-ARCH-MAP  
**Status:** **SEALED**  
**Motto:** *Ditempa Bukan Diberi* — Forged, Not Given

---

## 🧩 The 9 vs 13 Strategic Mapping

### 1. The Core Primitives (The 9)
The **Minimal Agent Kernel** required for any sovereign intelligence node.

| Primitive | Function | Tool Surface Implementation |
|:---:|:---|:---|
| **`boot`** | Session ignition & grounding | `anchor_session` |
| **`reason`** | Planning & causal logic | `reason_mind` |
| **`recall`** | Semantic continuity | `recall_memory` (Vector Memory) |
| **`simulate`** | Impact & consequence modelling | `simulate_heart` |
| **`critique`** | Uncertainty & blind-spot audit | `critique_thought` |
| **`judge`** | Governance verdict (Allow/Hold/Void) | `apex_judge` |
| **`execute`** | Action manifestation | `eureka_forge` |
| **`seal`** | Ledger persistence | `seal_vault` |
| **`audit`** | Self-reflection & alignment | `audit_rules` |

---

### 2. The Governed Surface (The 13)
The **Constitutional MCP Implementation** that mediates access to the 9 primitives through the **13 Constitutional Floors**, **2 Mirrors**, and **2 Walls**.

| MCP Tool | Kernel Primitive(s) | 13F Mediators | Risk Tier | 888_HOLD Condition |
|:---|:---|:---|:---|:---|
| `anchor_session` | `boot` | F11, F12 | LOW | N/A (Standard session) |
| `reason_mind` | `reason` | F2, F4, F7, F8 | MED | Broad strategic shift |
| `recall_memory` | `recall` | F2, F9 | LOW | Privacy breach detection |
| `simulate_heart`| `simulate` | F6 EMPATHY | LOW | High empathy deficit |
| `critique_thought`| `critique` | F7 HUMILITY | MED | Uncertainty spike > 0.1 |
| `apex_judge` | `judge` | **F13 SOVEREIGNTY** | **CRITICAL** | All irreversible/destructive |
| `eureka_forge` | `execute` | F1, F5, F12 | **HIGH** | Filesystem deletion, deploy |
| `seal_vault` | `seal` | F1, F11 | MED | Ledger mutation/rollback |
| `audit_rules` | `audit` | F2, F10 | LOW | Audit drift detection |
| `search_reality`| `sense (S1)` | F2, F12 | LOW | N/A (Read-only) |
| `fetch_content` | `sense (S2)` | F2 | LOW | N/A (Read-only) |
| `inspect_file` | `sense (S3)` | F2, F11 | MED | Protected folder read |
| `check_vital` | `telemetry` | F8, F5 | LOW | System instability |

---

### ⚖️ Shared Semantics & Delegated Judgment

- **`sense()`**: Realized as three distinct "reality" tools (`search`, `fetch`, `inspect`) rather than one monolithic primitive. This prevents "perception lock" and ensures multi-source grounding (F2 Truth).
- **`judge()`**: Explicitly decomposed. `apex_judge` handles the verdict semantics (SEAL/SABAR/VOID/888_HOLD) while the floors and walls enforce the criteria.
- **`execute()`**: Strictly scoped to `eureka_forge`. Direct imperative "run" commands are **locked (VOID)** unless filtered through the 000→999 metabolic pipeline.

---

## 📏 Constitutional Contracts

All interactions between the 9 Primitives and the 13 Surface Tools are governed by the following canonical envelopes:
1. `RequestEnvelope`: Intent grounding.
2. `VerdictEnvelope`: Final L4 decision.
3. `FloorScore`: Quantitative compliance metric.
4. `WitnessAttestation`: BFT consensus proof (F3 Witness).
5. `SealProof`: Merkle root of the vault entry.

*Ditempa Bukan Diberi* — **[CAPABILITIES SEALED]**
