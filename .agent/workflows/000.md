---
skill: "000"
version: "49.0.2"
description: Initialize arifOS Single Body Session (v49.0.2 aCLIP)
floors:
  - F1
  - F2
  - F7
  - F8
  - F11
allowed-tools:
  - Read
  - Bash(cat:*)
  - Bash(git log:*)
  - Bash(git status:*)
  - Bash(git branch:*)
  - Bash(date:*)
  - Bash(ls:*)
  - Bash(python:*)
expose-cli: true
derive-to:
  - codex
  - claude
  - antigravity
codex-name: arifos-workflow-000
claude-name: init-session
---
# /000 - Session Initialization Protocol v49.0

**Authority:** `docs/REPO_STRUCTURE_v49.md` — aCLIP Lingua Franca
**Version:** v49.0.2 (Single Body Runtime + aCLIP)

This workflow sets up the complete context for an arifOS v49 session, ensuring alignment with the Single Body Runtime and aCLIP protocol.

---

## 0. THE UNIFIED ORIGIN POINT (Read First)

**Before any initialization**: Understand the Single Body.

1. **Protocol Handshake**
   ```bash
   cat docs/REPO_STRUCTURE_v49.md
   # Understand: 000-999 Metabolic Stages & aCLIP Schema
   ```

2. **Load the Canon**
   ```bash
   ls 000_THEORY/
   # Confirm existence of 000_LAW.md and 000_ARCHITECTURE.md
   ```

---

## 1. ESTABLISH IDENTITY & TIME (Mandatory)

// turbo-all

3. **Check Time & Date** (Phoenix-72 Governor)
   ```bash
   date
   # Determines governance epoch and cooling periods
   ```

4. **Verify Agent Identity**
   ```bash
   # Confirm Ref: GEMINI.md
   cat GEMINI.md
   ```

---

## 2. SINGLE BODY WIRING (Mandatory)

5. **Verify v49 Wiring**
   ```bash
   python scripts/verify_v49_wiring.py
   # Checks: 000_THEORY, arifos_core, mcp_gateway, vault
   ```

6. **Load Environment**
   ```bash
   powershell -ExecutionPolicy Bypass -File scripts/load_env.ps1
   # Loads .env vars for the session
   ```

7. **Ignite Live Server** (Background)
   ```bash
   powershell -ExecutionPolicy Bypass -File scripts/boot_arifos.ps1 -Mode Cloud
   # Launches Unified Server in Cloud Mode (Port 8000)
   ```

---

## 3. CHECK REALITY (Mandatory)

8. **Check Git Status**
   ```bash
   git status
   ```

9. **Check Active Branch**
   ```bash
   git branch --show-current
   ```

10. **Check Trinity Seals**
    ```bash
    ls .git/seals/ 2>/dev/null && echo "Active Seals Found" || echo "No Seals Pending"
    ```

---

## 4. CHECK MEMORY (Mandatory)

11. **Read EUREKA Notes (Previous Session)**
    ```bash
    ls .antigravity/EUREKA_NEXT_SESSION.md 2>/dev/null
    ```

12. **Check Ledger State**
    ```bash
    tail -n 5 vault_999/BBB_LEDGER/LAYER_3_AUDIT/constitutional_ledger.jsonl 2>/dev/null || echo "Ledger not found or empty"
    ```

---

## Expected Output

After running `/000`, agent should know:

- ✅ **Protocol**: aCLIP v49 confirmed (`docs/REPO_STRUCTURE_v49.md`)
- ✅ **Identity**: Role & Authority confirmed (`GEMINI.md`)
- ✅ **Time**: Phoenix-72 Epoch confirmed
- ✅ **Wiring**: Single Body Runtime verified
- ✅ **Live**: MCP Server running on Port 8000 (`boot_mcp.ps1`)
- ✅ **State**: Git status & Seals checked

**Compliance Canary:** `[v49.0 | 12F | SINGLE BODY | aCLIP ALIGNED]`

---

## Next Steps

After `/000`, typically run:

**111 SENSE**:
- `/grep` or `/find` to orient within the codebase

**222 THINK**:
- `/plan` to design changes using the v49 metabolic stages

**Construction**:
- `/fag` to activate Full Autonomy Governance for implementation

---

**DITEMPA BUKAN DIBERI** — Forged, not given.
