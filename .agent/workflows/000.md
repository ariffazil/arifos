---
skill: "000"
version: "50.0.0"
description: Initialize arifOS Single Body Session (v50 aCLIP)
floors:
  - F1
  - F2
  - F7
  - F8
  - F11
allowed-tools:
  - Read
  - Bash(cat:*)
  - Bash(git log:*)
  - Bash(git status:*)
  - Bash(git branch:*)
  - Bash(date:*)
  - Bash(ls:*)
  - Bash(python:*)
expose-cli: true
derive-to:
  - codex
  - claude
  - antigravity
  - chatgpt
  - kimi
codex-name: arifos-workflow-000
claude-name: init-session
---
# /000 - Session Initialization Protocol v50.0

**Authority:** `docs/REPO_STRUCTURE_v50.md` — aCLIP Lingua Franca
**Version:** v50.0.0 (Single Body Runtime + AAA MCP)

This workflow sets up the complete context for an arifOS v50 session, ensuring alignment with the consolidated `arifos/` namespace and 33 AAA MCP tools.

---

## 0. THE UNIFIED ORIGIN POINT (Read First)

**Before any initialization**: Understand the Single Body.

1. **Protocol Handshake**
   ```bash
   cat docs/REPO_STRUCTURE_v50.md
   # Understand: 000-999 Metabolic Stages & aCLIP Schema
   ```

2. **Load the Canon**
   ```bash
   ls 000_THEORY/
   # Confirm existence of 000_LAW.md and 000_ARCHITECTURE.md
   ```

---

## 1. ESTABLISH IDENTITY & TIME (Mandatory)

// turbo-all

3. **Check Time & Date** (Phoenix-72 Governor)
   ```bash
   date
   # Determines governance epoch and cooling periods
   ```

4. **Verify Agent Identity**
   ```bash
   # Confirm Ref: GEMINI.md
   cat GEMINI.md
   ```

---

## 2. SINGLE BODY WIRING (Mandatory)

5. **Verify v50 Wiring**
   ```bash
   python scripts/verify_v49_wiring.py
   # Checks: 000_THEORY, arifos/, mcp_gateway, vault
   # Note: Script name retained for compatibility, validates v50 structure
   ```

6. **Load Environment**
   ```bash
   powershell -ExecutionPolicy Bypass -File scripts/load_env.ps1
   # Loads .env vars for the session
   ```

7. **Ignite Live Server** (Background)
   ```bash
   powershell -ExecutionPolicy Bypass -File scripts/boot_arifos.ps1 -Mode Cloud
   # Launches Unified Server on Port 8000 (33 AAA MCP Tools)
   ```

---

## 3. CHECK REALITY (Mandatory)

8. **Check Git Status**
   ```bash
   git status
   ```

9. **Check Active Branch**
   ```bash
   git branch --show-current
   ```

10. **Check Trinity Seals**
    ```bash
    ls .git/seals/ 2>/dev/null && echo "Active Seals Found" || echo "No Seals Pending"
    ```

---

## 4. CHECK MEMORY (Mandatory)

11. **Read EUREKA Notes (Previous Session)**
    ```bash
    ls .agent/EUREKA_NEXT_SESSION.md 2>/dev/null || ls .antigravity/EUREKA_NEXT_SESSION.md 2>/dev/null
    ```

12. **Check Ledger State**
    ```bash
    tail -n 5 vault_999/BBB_LEDGER/LAYER_3_AUDIT/constitutional_ledger.jsonl 2>/dev/null || echo "Ledger not found or empty"
    ```

---

## Expected Output

After running `/000`, agent should know:

- ✅ **Protocol**: aCLIP v50 confirmed (`docs/REPO_STRUCTURE_v50.md`)
- ✅ **Identity**: Role & Authority confirmed (`GEMINI.md`)
- ✅ **Time**: Phoenix-72 Epoch confirmed
- ✅ **Wiring**: Single Body Runtime verified (`arifos/` namespace)
- ✅ **Live**: MCP Server running on Port 8000 (33 AAA tools)
- ✅ **State**: Git status & Seals checked
- ✅ **Config**: `tools.yaml` indexed for model-agnostic access

**Compliance Canary:** `[v50.0 | 12F | SINGLE BODY | AAA MCP | 33 TOOLS]`

---

## Next Steps

After `/000`, typically run:

**111 SENSE**:
- `/111_sense` or `/111_search` to orient within the codebase

**222 THINK**:
- `/222_think` to design changes using the v50 metabolic stages

**Construction**:
- `/fag` to activate Full Autonomy Governance for implementation

---

## AAA MCP Tools Available

After initialization, the following tool groups are active:

| Server | Role | Tools | Stages |
|--------|------|-------|--------|
| AGI | Architect (Δ) | 18 | 111-333 |
| ASI | Auditor (Ω) | 5 | 555-666 |
| APEX | Judge (Ψ) | 8 | 444-889 |
| VAULT | Memory | 6 | 000, 999 |

---

**DITEMPA BUKAN DIBERI** — Forged, not given.
