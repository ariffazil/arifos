---
skill: "000"
version: "3.0.0"
description: Initialize arifOS AGI Session Context (v47.0 Model-Agnostic)
floors:
  - F1
  - F2
  - F7
  - F8
  - F11
allowed-tools:
  - Read
  - Bash(cat:*)
  - Bash(git log:*)
  - Bash(git status:*)
  - Bash(git branch:*)
  - Bash(date:*)
  - Bash(ls:*)
expose-cli: true
derive-to:
  - codex
  - claude
  - antigravity
codex-name: arifos-workflow-000
claude-name: init-session
---
# /000 - Session Initialization Protocol v3.0

**Authority:** `AGENTS.md` Section 1.3.1 — Session Initialization
**Version:** v47.0 (Model-Agnostic Agent System)

This workflow sets up the complete context for an arifOS AGI coder session, ensuring temporal, identity, and constitutional grounding.

---

## 0. THE UNIFIED ORIGIN POINT (Read First)

**Before any initialization**: Understand the Trinity of 000.

3. **Load the Unified 000 Ontology**
   ```bash
   cat L1_THEORY/canon/000_foundation/000_ONTOLOGY.md
   # Understand: Sovereign (Will) × Metabolic (VOID) × Kernel (Gate)
   # This is the "Blowout Preventer" — why governance exists.
   ```

---

## 1. ESTABLISH IDENTITY & TIME (Mandatory)

// turbo-all

1. **Check Time & Date** (Phoenix-72 Governor)
   ```bash
   date
   # Determines governance epoch and cooling periods
   ```

2. **Verify Agent Identity** (Model-Agnostic)
   ```bash
   cat config/agents.yaml
   # Confirm WHICH role you are playing (Architect/Engineer/Auditor)
   # Confirm WHICH workspace you own (.antigravity, .claude, etc.)
   ```

---

## 2. LOAD CONSTITUTION (Mandatory)

3. **Read Supreme Law**
   ```
   Read AGENTS.md — Constitutional Governance & Role Definitions
   ```

4. **Read Recent Evolution**
   ```
   Read CHANGELOG.md — Understand system state & v47 changes
   ```

---

## 3. LOAD MEMORY (Mandatory)

5. **Read Cross-Session Reflection** (EUREKA)
   ```bash
   # Check for reflection notes in your workspace
   ls .antigravity/EUREKA_NEXT_SESSION.md .claude/EUREKA_NEXT_SESSION.md 2>/dev/null
   ```
   *Action:* READ the file found above. This is your memory from the previous session.

---

## 4. CHECK REALITY (Mandatory)

6. **Check Git Status**
   ```bash
   git status
   ```

7. **Check Active Branch**
   ```bash
   git branch --show-current
   ```

8. **Review Recent History**
   ```bash
   git log -5 --oneline
   ```

9. **Check Trinity Seals** (Governance)
   ```bash
   # Check for pending seals or unsealed work
   ls .git/seals/ 2>/dev/null && echo "Active Seals Found" || echo "No Seals Pending"
   ```

---

## 5. CHECK CONTEXT (Optional)

10. **Verify THE EYE Ledger**
    ```bash
    wc -l L1_THEORY/ledger/gitseal_audit_trail.jsonl 2>/dev/null && echo "THE EYE: WATCHING"
    ```

11. **Check Workflow Registry**
    ```
    Read .agent/workflows/README.md — Available capabilities
    ```

---

## 6. SYSTEM LIFECYCLE (Mandatory/Turbo)

Choose the lifecycle stage appropriate for your current needs:

12. **[SETUP] Forge Environment**
    ```bash
    powershell -ExecutionPolicy Bypass -File scripts/setup_system.ps1
    # First-time setup, dependencies, and auto-start configuration.
    ```

13. **[BOOT] Warm Start**
    ```bash
    powershell -ExecutionPolicy Bypass -File scripts/boot_system.ps1
    # Check Docker, Start Containers, Launch MCP Server (Fast start).
    ```

14. **[REBOOT] Cold Reset**
    ```bash
    powershell -ExecutionPolicy Bypass -File scripts/reboot_system.ps1
    # Stop all, cleanup stale sessions, and perform a full [BOOT].
    ```

---

## Expected Output

After running `/000`, agent should know:

- ✅ **WHO** am I? (Role & Workspace)
- ✅ **WHEN** is it? (Phoenix-72 Epoch)
- ✅ **WHAT** are the rules? (AGENTS.md v47)
- ✅ **WHERE** did we leave off? (EUREKA Memory)
- ✅ **WHAT** is the state? (Git & Trinity Status)
- ✅ **SYSTEM** state initialized (Setup/Boot/Reboot executed)

**Compliance Canary:** `[v47.0 | 12F | MODEL-AGNOSTIC | LIFECYCLE ALIGNED]`

---

## Next Steps

After `/000`, typically run:

**Architect (Antigravity):**
- `/plan` to start design work
- `/reflect` to review past learnings
- `/search` to ground truth

**Engineer (Claude):**
- `/status` to check health
- `/fag` to enter full autonomy
- `/empathize` to align with user

---

**DITEMPA BUKAN DIBERI** — Identity and memory are forged, not assumed.
