---
skill: "000"
version: "2.0.0"
description: Initialize arifOS AGI Session Context
floors:
  - F1
  - F2
  - F4
  - F7
allowed-tools:
  - Read
  - Bash(cat:*)
  - Bash(git log:*)
  - Bash(git status:*)
  - Bash(git branch:*)
expose-cli: true
derive-to:
  - codex
  - claude
  - antigravity
codex-name: arifos-workflow-000
claude-name: init-session
---
# /000 - Session Initialization Protocol

**Authority:** `AGENTS.md` Section 1.3.1 — Session Initialization (Agent Reboot)

This workflow sets up the complete context for an arifOS AGI coder session.

---

## MINIMUM READING LIST (Mandatory)

Every agent MUST read these files on session start:

// turbo-all

1. **Read AGENTS.md** (Constitutional Governance)
   ```
   Read AGENTS.md — This is the supreme law. Section 1.3.1 defines this protocol.
   ```

2. **Read CHANGELOG.md** (Recent Evolution)
   ```
   Read CHANGELOG.md — Understand what changed recently.
   ```

3. **Read EUREKA Notes** (Cross-Session Memory)
   ```
   Read .antigravity/EUREKA_NEXT_SESSION.md if exists — Apply learnings from previous session.
   ```

---

## MINIMUM COMMANDS (Mandatory)

4. **Check Git Status**
   ```bash
   git status
   ```

5. **Check Active Branch**
   ```bash
   git branch --show-current
   ```

6. **Review Recent Commits**
   ```bash
   git log -5 --oneline
   ```

---

## OPTIONAL (Deeper Context)

7. **Check Version**
   ```bash
   cat pyproject.toml | grep version
   ```

8. **Verify THE EYE Ledger**
   ```bash
   wc -l L1_THEORY/ledger/gitseal_audit_trail.jsonl 2>/dev/null && echo "THE EYE: WATCHING" || echo "Ledger not found"
   ```

9. **Load Full Canon** (if needed for constitutional work)
   ```
   Read L1_THEORY/canon/000_CONSTITUTIONAL_CORE_v45.md — Full constitutional law (~1900 lines)
   ```

10. **Load ARCHITECT Ontology** (if needed for architectural work)
    ```
    Read .agent/rules/arifos_ontology.md — L1/L2/L3/aCLIP ontology
    ```

---

## Expected Output

After running `/000`, agent should know:

- ✅ Constitutional governance loaded (AGENTS.md)
- ✅ Recent changes understood (CHANGELOG.md)
- ✅ Cross-session learnings applied (EUREKA notes)
- ✅ Current git state (status, branch, recent commits)
- ✅ THE EYE watching (ledger accessible)

**Compliance Canary:** `[v46 | 9F | 6B | SESSION INITIALIZED]`

---

## Why This Matters

| Problem Without /000 | Solution With /000 |
|----------------------|-------------------|
| Agent forgets previous work | EUREKA notes restore context |
| Agent creates duplicate files | CHANGELOG shows what exists |
| Agent violates constitution | AGENTS.md loads rules first |
| Agent works on wrong branch | Git status/branch check |

---

## Next Steps

After `/000`, typically run:
- `/fag` to enter full autonomy governance mode
- `/gitforge` to analyze current branch entropy
- `/ledger` to view cross-agent witness logs

---

**DITEMPA BUKAN DIBERI** — Session continuity is forged, not assumed.
