---
description: 111 SENSE - Pattern Matching & Search (v50 aCLIP)
---
# 111 SENSE: Pattern Recognition

1. **Scan**: `grep_search` or `find_by_name` for relevant files.
2. **Read**: `view_file` on high-relevance targets.
3. **Map**: Identify dependencies and context.
4. **Lane**: Determine `applicability_lane` (e.g., PHATIC, SOFT, HARD).

Output: `111_SENSE` complete. Proceed to `222_THINK`.
