"""
canonical_core/__init__.py — Canonical Core Exports
"""

# Export main classes for easy access
# We use relative imports inside the package, but expose them cleanly here

# Note: We rely on the package structure being correct. 
# If running as a script, ensure PYTHONPATH includes the root directory.