# AAA MCP Tools Reference (Redirect)

This document is intentionally reduced to avoid tool-surface drift.

Canonical source of truth:
- `TOOLS.md` (repo root)

Why this redirect exists:
- Historical copies in multiple locations caused naming mismatch (`recall_memory` vs `vector_memory`, etc.).
- The project now maintains one authoritative tool catalog.

For protocol-level schemas:
- `arifosmcp/transport/protocol/schemas.py`

For runtime registration:
- `arifosmcp/transport/server.py`
