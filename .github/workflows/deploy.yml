name: Deploy Trinity Ecosystem

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
<<<<<<< HEAD
  # --- Step 1: Build and Deploy Static Frontend Layers to Cloudflare Pages ---
  deploy-frontend:
=======
  # --- Step 1: Deploy Static Frontend Layers to Cloudflare Pages ---
  # BODY (arif-fazil.com) and SOUL (apex.arif-fazil.com) are React apps
  # deployed to Cloudflare Pages for fast, reliable static hosting
  deploy-frontend:
    name: Deploy Frontend (Cloudflare Pages)
>>>>>>> 46a57cb9 (888_DECREE: Ecosystem Alignment - Cloudflare Pages Transition)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'

<<<<<<< HEAD
      # Build and Deploy BODY (arif-fazil.com)
      - name: Build BODY (arif-fazil.com)
=======
      # Deploy SOUL (apex.arif-fazil.com) - Constitutional Canon
      - name: Build SOUL (apex.com)
        run: |
          cd apex.com/app
          npm ci
          npm run build
          # Copy all image assets to dist
          cp public/*.jpg dist/ 2>/dev/null || true
          cp public/*.png dist/ 2>/dev/null || true

      - name: Deploy SOUL to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: apex-dpf
          directory: apex.com/app/dist

      # Deploy BODY (arif-fazil.com) - Identity & Dashboard
      - name: Build BODY (ariffazil.com)
>>>>>>> 46a57cb9 (888_DECREE: Ecosystem Alignment - Cloudflare Pages Transition)
        run: |
          cd ariffazil.com/app
          npm ci
          npm run build
<<<<<<< HEAD
          cp ../*.jpg dist/ 2>/dev/null || true
          cp ../three-judges-geometric.jpg dist/ 2>/dev/null || true
          cp ../13-floors-geometric.jpg dist/ 2>/dev/null || true
          ls -la dist/
=======
          # Copy all image assets to dist
          cp public/*.jpg dist/ 2>/dev/null || true
          cp public/*.png dist/ 2>/dev/null || true
>>>>>>> 46a57cb9 (888_DECREE: Ecosystem Alignment - Cloudflare Pages Transition)

      - name: Deploy BODY to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: arif-fazil
          directory: ariffazil.com/app/dist

<<<<<<< HEAD
      # Build and Deploy SOUL (apex.arif-fazil.com)
      - name: Build SOUL (apex.arif-fazil.com)
        run: |
          cd apex.com/app
          npm ci
          npm run build
          cp ../public/*.jpg dist/ 2>/dev/null || true
          ls -la dist/

      - name: Deploy SOUL to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: apex-dpf
          directory: apex.com/app/dist

  # --- Step 2: Deploy MIND (Backend) to Railway ---
  deploy-backend:
=======
  # --- Step 2: Deploy Backend API to Railway ---
  # MIND layer (FastAPI/MCP) hosts the constitutional tools
  # This runs only after frontend deployment succeeds
  deploy-backend:
    name: Deploy Backend (Railway)
>>>>>>> 46a57cb9 (888_DECREE: Ecosystem Alignment - Cloudflare Pages Transition)
    needs: deploy-frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

=======
>>>>>>> 46a57cb9 (888_DECREE: Ecosystem Alignment - Cloudflare Pages Transition)
      - name: Setup Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy MIND to Railway
<<<<<<< HEAD
        run: railway up --service arifos-mind --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: Verify Backend Health
        run: |
          sleep 10
          curl -sf https://arifos.arif-fazil.com/health || echo "Health check will be available after DNS propagation"
=======
        run: railway up --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

  # --- Step 3: Verify Trinity Alignment ---
  verify:
    name: Verify Ecosystem Alignment
    needs: [deploy-frontend, deploy-backend]
    runs-on: ubuntu-latest
    steps:
      - name: Check BODY (arif-fazil.com)
        run: |
          echo "Checking BODY layer..."
          curl -sf https://arif-fazil.com > /dev/null && echo "✅ BODY is ONLINE" || echo "⚠️ BODY check failed"

      - name: Check SOUL (apex.arif-fazil.com)
        run: |
          echo "Checking SOUL layer..."
          curl -sf https://apex.arif-fazil.com > /dev/null && echo "✅ SOUL is ONLINE" || echo "⚠️ SOUL check failed"

      - name: Check MIND (Railway Health)
        run: |
          echo "Checking MIND layer..."
          curl -sf https://arif-fazil.com/health > /dev/null && echo "✅ MIND is ONLINE" || echo "⚠️ MIND check failed"

      - name: Trinity Alignment Report
        run: |
          echo "========================================"
          echo "  TRINITY ECOSYSTEM ALIGNMENT REPORT"
          echo "========================================"
          echo "  BODY:  https://arif-fazil.com"
          echo "  SOUL:  https://apex.arif-fazil.com"
          echo "  MIND:  https://arifos.arif-fazil.com"
          echo "========================================"
          echo "  DITEMPA BUKAN DIBERI"
          echo "========================================"
>>>>>>> 46a57cb9 (888_DECREE: Ecosystem Alignment - Cloudflare Pages Transition)
