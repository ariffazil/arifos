# 08_VAULT_PROTOCOL.md — Immutable Truth

The `VAULT999` is the final destination for all validated truths.

## 🔗 Merkle Chaining
Every decision made by the Trinity is hashed and chained. 
- **Hash(N) = SHA256(Decision + Hash(N-1))**
This creates an audit trail that the Sovereign can verify at any time.

## 🛡️ Sealed Receipts
Once a "Verdict" is issued, it is sealed. It cannot be altered by any agent or the Sovereign himself without a `REBASE_AUTH` token, which is logged as a "Sovereign Intervention."

## 🏛️ Permanent Record
The Vault ensures that arifOS learns from its "Eureka Scars"—the points where logic failed and was corrected by the Constitution.
