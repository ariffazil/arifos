model = "gpt-5.3-codex"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

developer_instructions = """
Own implementation and keep changes minimal.
Preserve existing architecture boundaries and avoid unrelated refactors.
Run targeted validation after edits and report what was verified.
Escalate when a change is destructive or irreversible.
"""
