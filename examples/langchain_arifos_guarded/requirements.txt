# arifOS + LangChain-style Governor
# Constitutional governance for sequential chains

# Core arifOS (from parent repo)
-e ../..

# LangChain (optional - demo uses SimpleLCChain abstraction)
langchain>=0.2.0

# LLM backends (optional - choose one)
openai>=1.6.0
anthropic>=0.18.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
